import { DefaultTheme } from 'styled-components/native';

const customTheme: DefaultTheme = {
  colors: {
    mainBg: '#59748e',
    white: '#FFFFFF',
    black: '#000000',
    blacklight: '#575757',
  },
};

export { customTheme };
