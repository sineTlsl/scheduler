import { DefaultTheme } from 'styled-components/native';

const theme: DefaultTheme = {
  colors: {
    mainBg: '#59748e',
    white: '#FFFFFF',
    black: '#000000',
  },
  fonts: {
    normal: '14px',
  },
};

export default theme;
